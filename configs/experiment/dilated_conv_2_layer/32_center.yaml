# @package _global_

defaults:
  - /model/backbone/dilated_conv_2_layer
  - /model/unsupervised/linear_head
  - override /model/loss: mse
  - override /data: raw_data
  - override /callbacks: unsupervised
  - override /trainer: unsupervised_fp32

data:
  size_power: 5
  devel_batch_size: 2048
  train_batch_size: 1024

model:
  backbone_out_feat: 64

supervised: False
run_test: False